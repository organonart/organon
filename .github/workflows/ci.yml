# Organon — CI
#
# Builds and tests the native engine. #626 Tier 1.
#
# ── What this can and cannot tell you ───────────────────────────────────────────
# It can tell you: it compiles, the unit tests pass, and every WGSL shader parses
# and validates (naga, on the CPU — `native/tests/wgsl.rs`). It cannot tell you
# whether anything renders, how fast, whether the plugin loads in a host, or
# whether live inference works. A green run means **"green and ready to deploy"**,
# never "verified working". The Mac and a GPU-capable host own the rest.
#
# ── Why every job, and why none is redundant ────────────────────────────────────
# Two axes, not one: the EDITION (a default-off cargo feature) and the PLATFORM.
# (This heading used to say "four jobs" and was already stale by one, then said
# "every edition" and was stale again the day Mind stopped being one. Read the
# legs off the `jobs:` block; a number or a list written in prose here only ever
# rots.)
#
# `console-edition` is a default-OFF cargo feature, so the two Linux builds cover
# disjoint code and each is blind where the other sees:
#
#   default          covers the plugin cdylib, the standalone, `frame_ring`,
#                    `viewport_pane` — and compiles NONE of `src/console_main.rs`.
#   console-edition  covers the Console front-of-house and the `Edition::Console`
#                    arms — and compiles none of the
#                    `cfg(not(feature = "console-edition"))` ones.
#
# That asymmetry is the hazard CLAUDE.md warns about in prose ("you can break an
# edition and see a green suite"). Running both here is what makes the warning
# unnecessary. Do not collapse them into one job to save minutes — the saving
# would be the entire point of the check.
#
# ⚠️ **THERE IS NO `build (mind-edition)` LEG ANY MORE, AND ITS ABSENCE IS A
# DECISION, NOT AN OMISSION.** Organon Mind is no longer a separate product or a
# separate build — there is one Organon, plus the VST it exports — so an edition
# leg policing a second front-of-house had nothing left to police. Removed
# 2026-08-21; it cost ~6 minutes of Linux runner per PR (measured below) and its
# cross-check step another ~15 s.
#
# What is deliberately NOT claimed here: the `mind-edition` cargo feature and the
# `organon-mind` package still exist in `native/`. The package is a workspace
# member, so `cargo test --release --workspace` in the default leg below still
# builds it and still runs its tests — that coverage did not move. What is gone is
# the leg that turned the *feature* on. If that feature is ever load-bearing again,
# the leg comes back; until the feature is actually deleted from `Cargo.toml`, a
# change that breaks `--features mind-edition` will land green here.
#
# ── The platform axis (organon#658 Tier 1) ──────────────────────────────────────
# Windows is a first-class target: Organon ships there, plugin included. The two
# legs above run on `ubuntu-latest` and cannot see a Windows regression, so a
# `cfg(unix)` assumption creeping into a shared file would land green. Two more
# jobs close that, and they are deliberately ASYMMETRIC because they cost wildly
# different amounts:
#
#   check (windows cross)   ubuntu-latest. `cargo check --target
#                           x86_64-pc-windows-msvc`, BOTH editions, all targets.
#                           1x billing on a fast runner.
#   build (windows)         windows-latest. Real build + real `cargo test`,
#                           default edition only.
#
# ⚠️ THE CROSS-CHECK NEEDS NO SYSTEM PACKAGES, AND THAT IS NOT AN OVERSIGHT. It
# is the single most surprising fact about this workflow, so it is written down
# rather than left to look like a missing step:
#
#   * `native/` has NO `build.rs` anywhere in the workspace, so the default build
#     compiles no C/C++ at all.
#   * The Linux legs' apt step exists for ALSA/JACK (nih-plug's standalone audio
#     backends) and X11/GL (baseview). On Windows those resolve to WASAPI and
#     Win32 through pure-Rust binding crates — no headers to install.
#   * `cargo check` type- and borrow-checks WITHOUT LINKING, so no MSVC SDK and
#     no import libraries are needed either.
#
# Measured 2026-08-07 in a fresh Linux container: a native `cargo check` FAILS
# there (`alsa-sys` build script, no headers) while the Windows cross-check
# passes clean, both editions. Windows is currently the CHEAPEST target to prove
# compiles. That is also why contributors can run it locally in seconds — see the
# contributor note below.
#
# ⚠️ WHAT THE CROSS-CHECK CANNOT SEE, so nobody reads it as more than it is: it
# does not link, does not run a test, and knows nothing about runtime behaviour.
# Every defect organon#658 Tier 1 fixes — `.exists()` on a path missing `.exe`, a
# hardcoded `Library/Application Support`, `set_len` on a mapped file — is
# invisible to it. It catches the "someone typed `std::os::unix` in a shared
# file" class and nothing else. `build (windows)` is what links and runs tests;
# the workstation is what judges pixels.
#
# ── Spend, stated because this file's own rule is to ask ────────────────────────
# The block below already rules that bigger runners and cache-warming jobs are
# "spend decisions, not code ones — ask before adding either". A Windows runner
# is the same kind of decision, so here is the arithmetic rather than a surprise
# on the next invoice: GitHub bills `windows-latest` at **2x** the per-minute
# rate of Linux and it is slower on the I/O-heavy work cargo does, so ONE Windows
# leg costs roughly 4x one Linux leg.
#
#   ► THE CHOICE MADE HERE: **one** Windows job, default edition only, plus the
#     cheap cross-check covering BOTH editions on Linux. The Console's Windows
#     compile coverage therefore comes from the 1x job, not the 4x one.
#
# That asymmetry is a considered trade, not an omission. `cargo test --workspace`
# on the default edition already runs everything platform-sensitive — the
# `organon-core` IPC suite, the seqlock tests, the layout goldens — because those
# live in a workspace member both editions share. What `console-edition` adds on
# top is compositor and front-of-house logic that is pure and platform-independent.
# So the second Windows job would cost another 4x to re-run platform-agnostic
# tests on a platform that cannot make them behave differently.
#
# If a Windows-specific Console defect ever DOES slip through, that trade is what
# let it, and the fix is to promote the cross-check job to a second
# `windows-latest` build+test leg. Do that deliberately, with the cost above in
# hand — do not let it happen by accident.
#
# ── macOS, and why it does NOT get the cheap half ───────────────────────────────
# Until this leg, NOTHING in CI proved macOS for ANY edition — the platform the
# plugin is actually developed and deployed on (`deploy.sh` is macOS-only) had zero
# compile coverage. `build (macos)` closes that for the Console.
#
# 🚨 **THE WINDOWS CROSS-CHECK TRICK DOES NOT TRANSFER TO APPLE TARGETS, AND THE
# REASON IS NOT THE ONE THE BLOCK ABOVE WOULD LEAD YOU TO EXPECT.** Read that block
# and you would conclude a `check --target aarch64-apple-darwin` leg must work too:
# no `build.rs` in `native/`, and `cargo check` does not link. Both facts are still
# true, and the check still fails — because the blocker is a build script in a
# DEPENDENCY, and `cargo check` runs those:
#
#     error: failed to run custom build command for `coreaudio-sys v0.2.18`
#     coreaudio.h:1:10: fatal error: 'AudioUnit/AudioUnit.h' file not found
#
# `nih_plug` (features = ["standalone"]) → `cpal` → `coreaudio-rs` →
# `coreaudio-sys`, whose build script runs **bindgen against the macOS SDK
# headers**. There is no SDK on a Linux runner, so it dies before a single line of
# our code is type-checked. Windows has no counterpart: its audio and windowing
# reach WASAPI/Win32 through pure-Rust binding crates with nothing to generate.
#
# ⚠️ The `-sys` crates are NOT interchangeable in this respect and it is worth
# knowing which is which before someone "fixes" the wrong one. `jack-sys` — also in
# the Unix graph, also a `-sys` crate — cross-checks **clean** from Linux, because it
# is `dlopen`-based (`libloading`) and needs no headers at all. Measured 2026-08-16 in
# this container: `cargo check --target aarch64-apple-darwin -p jack-sys` exits 0.
# `coreaudio-sys` and `coremidi-sys` are the bindgen ones, and they are the wall.
#
#   ► THE CHOICE MADE HERE: **one macOS job, no Apple cross-check leg beside it.**
#
# The cross-check leg is not merely blocked — even the version that WOULD work is
# not worth a job. **Two** of the workspace's eleven members reach nih_plug and are
# therefore blocked: the root crate `organic-math-native`, and `organon-visual`,
# which depends on it. The other nine cross-check clean for `aarch64-apple-darwin`,
# all targets, in well under a minute. But its coverage is a strict subset of what
# the real macOS leg already sees, and the leg is free here. So the partial
# cross-check lives in the contributor note below, as an inner loop, not as a job.
#
# ⚠️ **Write that exclusion as a DENY-list, never an allow-list**, for the same
# reason `paths-ignore` below is one: a member added later must be covered by
# default, not silently omitted. `-p a -p b -p c…` is the shape the `--workspace`
# note further down exists to forbid, and it failed in exactly the predicted way
# while this leg was being written — the first version of the recipe was typed from
# CLAUDE.md's repository map and silently missed five real members
# (`organon-agent`, `organon-visual`, `organon-world`, `xtask`, the vendored
# `egui-wgpu`). `cargo metadata --no-deps` is the authority on who the members are;
# a prose list anywhere, this comment included, is not.
#
# ── Spend for the macOS leg ─────────────────────────────────────────────────────
# The Windows block above states its arithmetic in billing multipliers, so state
# this one the same way and then state the thing that actually governs it:
#
#   * **This repository is PUBLIC, so standard GitHub-hosted runners are billed at
#     $0 — Linux, Windows and macOS alike.** The 2x/10x multipliers are what these
#     minutes *would* cost on a private repo, and they are the right shape to
#     reason with if that ever changes. Today they are not the constraint.
#   * `macos-latest` is the 3-core arm64 (M-series) standard runner. It is billed at
#     10x on a private repo — the most expensive minute Actions sells — but it is
#     also genuinely fast at the I/O-heavy work cargo does, unlike `windows-latest`.
#   * The real constraint on a public repo is **concurrency**, not money: macOS
#     runners have a much smaller concurrent-job allowance than Linux, so a macOS
#     leg is the one most likely to sit queued. That is a wall-clock cost on a busy
#     day, and it is the reason to add one macOS job rather than three.
#
# On this leg's first-ever run (PR #96, cold cache) it did **not queue** — the runner
# was allocated in 1 s alongside the other five — so the concurrency caveat above is a
# risk to plan for, not something observed. Its current timing is in the one table
# below; the thing to watch is its **test** half, which runs ~50% longer than the Linux
# legs' on fewer cores and is what would need trimming first.
#
# ► **Console edition only, and build + test rather than a check.** Only-one-job is
#   the concurrency argument above. Build-and-test rather than check is because this
#   is the ONLY leg that can execute anything at all on macOS — a `check` here would
#   buy the cheap half of a signal on the one platform where nothing was linking or
#   running to begin with. And the edition is the Console's because that is what
#   this leg was added to establish.
#
# ⚠️ **The default edition still has NO macOS coverage, and that is a gap, not a
# considered trade like the Windows/Console one above.** It is the sharper gap of
# the two now that there are only two editions, because the default edition is what
# carries the VST3/CLAP export — on the platform where the plugin is actually
# developed and deployed. Two things soften it and neither closes it:
# `--bin organon-console` builds the root crate's **lib** as well, so almost all
# shared macOS ground (nih_plug, wgpu/Metal, winit, `window_macos.rs`) is compiled by
# this leg anyway; and `cargo test --workspace` runs every workspace member's suite on
# macOS regardless of edition. What is genuinely uncovered is the
# `cfg(not(feature = "console-edition"))` arms — the plugin cdylib's own export macros
# and `standalone.rs`. Promoting a second macOS leg is a deliberate decision with the
# concurrency cost above in hand; do not let it happen by accident, and do not read
# this leg as proving the plugin builds on a Mac.
#
# ── Native only ─────────────────────────────────────────────────────────────────
# There is nothing here but `native/`, so there is nothing else to build. This
# repository is Rust and its documentation — no npm, no TypeScript, no build step
# outside cargo (`native/tools/*.py` are hand-run tools, not a build step). If a
# second buildable tree ever lands, it gets its own workflow, not a leg here.
#
# ── Contributors ────────────────────────────────────────────────────────────────
# To reproduce a leg locally, run the two `run:` lines of the job you care about
# from `native/`, with `CARGO_PROFILE_RELEASE_LTO=false` exported. On a fresh
# Linux box install the dev headers from the apt step first — without them the
# build dies inside a *build script* and the real error ("system library
# `x11-xcb` not found") is buried in that script's stderr, far below a wall of
# `rerun-if-env-changed` lines.
#
# The Windows cross-check is the exception and needs none of that — two commands
# from any Linux or macOS box, no Windows machine, no MSVC SDK, no apt:
#
#   rustup target add x86_64-pc-windows-msvc
#   cd native && cargo check --target x86_64-pc-windows-msvc --workspace --all-targets
#
# Use it as the inner loop for any `cfg(windows)` work — it is fast and it means
# the Windows arms are compiler-verified rather than written blind. Read the
# caveat above before quoting a green result at anyone.
#
# **The Apple equivalent exists but is PARTIAL, and the partiality is the whole
# point of knowing about it.** From any Linux or macOS box, no Mac and no Xcode:
#
#   rustup target add aarch64-apple-darwin
#   cd native && cargo check --target aarch64-apple-darwin --all-targets \
#     --workspace --exclude organic-math-native --exclude organon-visual
#
# ⚠️ Note the shape: `--workspace` minus the two members that reach nih_plug, NOT a
# `-p` list of the nine that do not. A member added later is then checked by
# default; with an allow-list it would be silently skipped. `organon-visual` is on
# the deny side only because it depends on the root crate — it is not itself a
# nih_plug crate.
#
# That covers nine of eleven members — which is most of the Console — in well under
# a minute, and it is the right inner loop for compositor work. What it CANNOT do is
# reach the root crate: `cargo check` on `organic-math-native` for an Apple target
# dies in `coreaudio-sys`'s build script with `'AudioUnit/AudioUnit.h' file not
# found`, for the reason the macOS block above sets out. So `console_main.rs` — the
# Console's actual entry point and window — is invisible to it. Only `build (macos)`
# compiles that. Do not grow the excludes into a substitute for the real leg.

name: CI

# ── Trigger: pull_request only. No `push:`, deliberately. ───────────────────────
# Every commit reaching `main` arrives through a PR that was already green, so a
# push trigger would re-run this whole matrix a second time on the merge result
# and roughly double the spend for a near-duplicate signal. `workflow_dispatch`
# stays so the matrix can be run by hand after a merge if something looks off.
# (#626, the maintainer's ruling 2026-08-04.)
#
# Accepted consequence #1: a merge commit is never tested *as* a merge commit. If
# a semantic conflict ever slips through, the answer is a `merge_group` trigger —
# not a blanket `push`.
#
# ⚠️ Accepted consequence #2: EVERY RUN REBUILDS THE CRATE, and the first run of
# each PR also rebuilds its dependencies. Neither is a misconfiguration of the
# cache below, so do not go looking for the bug there. Two separate reasons:
#
# 1. `Swatinem/rust-cache` caches DEPENDENCIES ONLY. It deliberately prunes the
#    workspace crate's own artifacts before saving, because a workspace crate
#    changes with every commit and its artifacts are stale the moment they are
#    written. So `organic-math-native` — the 125k-line crate, and the bulk of the
#    ~9 minutes below — is recompiled on EVERY run, warm cache or not. What the
#    cache buys is the dependency graph (~400 crates), which is real but is not
#    the dominant term.
#
# 2. GitHub scopes a cache written by a `pull_request` run to the merge ref
#    (`refs/pull/N/merge`); a run may restore only from its own scope, its base
#    branch, or the default branch. With no `push:` trigger nothing ever writes
#    into the default-branch scope, so:
#
#      • re-runs and later pushes on the SAME PR  → dependencies restored
#      • a different PR, or the first run of one  → dependencies rebuilt too
#
# Measured on this workflow's own first two runs (PR #631, both legs in parallel):
# the build step went **9m19s cold → 4m01s warm, −57%**. So the split is roughly ~4
# minutes of crate (recompiled always, per reason 1) and ~5 minutes of dependency
# graph (cached, restored on same-PR re-runs). The cache earns its keep — it just
# cannot take the build to zero, and the residue is the crate, not a
# misconfiguration.
#
# ── What each leg actually costs ────────────────────────────────────────────────
# Run 32443462639 (PR #105, 2026-08-21), every leg green, warm cache. One table for
# all of them rather than a figure buried in each section, because the only useful
# question is which leg sets the wall clock:
#
#                            BUILD      TEST     JOB TOTAL
#   build (default)          2m22s      4m46s      7m45s
#   build (console-edition)  2m21s      4m26s      7m26s
#   check (windows cross)      49s over 3 check steps        1m18s  ← now 2 steps
#   build (windows)          8m53s     12m17s     22m05s   ← sets the wall clock
#   build (macos)            3m12s      7m00s     10m44s
#   ── removed 2026-08-21, listed once so the saving is on the record ──
#   build (mind-edition)     1m53s      3m28s      5m58s
#
# Whole-run wall clock: **22m31s, set by `build (windows)` alone.** Read the two
# consequences off that and not off the totals column:
#
#   * Dropping the Mind leg buys ~6 minutes of Linux runner and ~15 s of the
#     cross-check per PR, and moves the wall clock **not at all** — the legs run in
#     parallel and Windows is 3x the next slowest. It went because it checked a
#     product that no longer exists, never because it made anyone wait.
#   * The only leg worth optimising for time is the Windows one, and its cost is
#     the *test* step, not the build. Nothing here is close to
#     `timeout-minutes: 90`; that stays a runaway backstop with ~4x headroom on the
#     slowest leg, not a budget anyone is spending.
#
# The build dominates on every Linux leg; the test step is NOT the risk the mirror's
# SIGTERM lesson warned about, so long as LTO stays off (see the env block).
#
# If the spend ever does become binding, the lever is NOT a bigger cache — see
# reason 1, the crate is uncacheable by design. It is either a build-only
# cache-warming job on `push: branches: [main]` (seeds the default-branch scope,
# helping every PR's first run) or a larger runner. Both are spend decisions, not
# code ones — ask before adding either.
#
# ⚠️ PATH FILTERS AND REQUIRED CHECKS DEADLOCK. `paths-ignore` means a docs-only
# PR never starts these jobs, so the checks never report. If they are ALSO marked
# *required* in branch protection, GitHub waits forever for a report that will
# never come and the PR can never merge.
#
#   ► THE CHOICE MADE HERE: **these checks are NOT marked required.** They are
#     advisory — read the run, then merge. Nothing in branch protection blocks on
#     `build (default)` or any other leg.
#
# If you ever want them required, you must first break the deadlock, and there are
# only two ways: drop `paths-ignore` entirely (every PR pays the matrix), or add a
# second workflow with the same `name:` and job names, the *inverse* path filter,
# and a body that just reports success. Do not mark them required and leave this
# block as-is.
on:
  pull_request:
    # A deny-list, not an allow-list — the reverse of the export manifest, and on
    # purpose. For the export, default-deny means new code stays private, which is
    # safe. For CI, default-deny would mean new code is silently untested, which is
    # not. So a new top-level directory gets tested until someone says otherwise.
    # Everything listed here is prose or agent configuration — none of it can change
    # the outcome of `cargo build`.
    #
    # ⚠️ **The list is SHORTER than it was, and shorter is the correct direction.**
    # It used to carry eight more entries — `site/`, `site-mind/`, `web/`, `src/`
    # (the legacy React-Three-Fiber app), `brand/`, `songs/`, `original_code/` and
    # the npm/vite config files. Not one of those paths exists in this repository;
    # they crossed over with the file. A pattern for a directory that is not here
    # does nothing today and, the day someone adds that directory back, silently
    # exempts it from CI — which is exactly the default-deny failure the paragraph
    # above rejects. Removed 2026-08-21. Do not re-add a pattern speculatively; add
    # it when the directory lands, with a line saying why it cannot affect cargo.
    #
    # ⚠️ These patterns are anchored at the REPO ROOT, except `**.md`, which is not
    # and is not meant to be: it matches at any depth, so `native/**/*.md` is ignored
    # too. Nothing else under `native/` is ignored by anything here — worth stating
    # rather than assuming, because if it were wrong every pure-Rust PR would
    # silently run nothing.
    #
    # ⚠️ **The filter is evaluated against the PR's WHOLE diff, not the newest push**
    # (observed on #677, 2026-08-08: a commit touching only two `.md` files re-ran
    # every leg, because the PR also carried `native/src/lib.rs`). So "docs-only" is a
    # property of the *pull request*, not of the commit you just pushed:
    #   * a PR whose entire diff is prose never starts these jobs — that is the
    #     deadlock case the block above is about;
    #   * a code PR pays the full matrix again for every follow-up commit, including a
    #     CHANGELOG fix. That is not a misconfiguration, and it is the reason a
    #     docs-only fixup to an open code PR is not free. Batch them if it matters.
    paths-ignore:
      - 'doc/**'             # PRD, papers, shop notes, the research evals
      - '.claude/**'         # hooks, settings, and organon-cli (the skill's one home)
      - '**.md'              # CLAUDE.md, ARCHITECTURE.md, CHANGELOG.md, changelog.d/…
      # Added by #626 T2 after the filter's first miss: that PR changed only
      # `.claude/**` + `**.md` + this file, and CI ran anyway — two full matrix
      # legs for one ignored line. `.gitignore` meets the criterion above, and
      # the reason is worth stating because it is the opposite of the intuition:
      # `actions/checkout` materialises TRACKED files, and .gitignore has no say
      # over those. It governs what git *adds*, and CI adds nothing. Note this is
      # the ROOT .gitignore only — `native/.gitignore` is a different file and
      # equally inert here, but it is covered by neither this line nor any other,
      # which is fine: nothing has ever changed it in a Rust-free PR.
      - '.gitignore'
      # ⚠️ ONE CARVE-OUT, and it must stay LAST — negations override the patterns
      # above them, so moving this line up silently disarms it.
      #
      # `doc/reference/**` is not prose, it is BUILD OUTPUT: generated by
      # `organon docs` from the descriptions in `agent.rs`/`recipe.rs`, and pinned
      # by `generated_reference_is_current` in `native/src/cli.rs`. A change to a
      # description is a `native/src/**` change and already runs the matrix, so the
      # direction that matters (code moves, docs go stale) is covered either way.
      # This closes the reverse: a PR that hand-edits the generated Markdown and
      # nothing else. Without it such a PR matches both `doc/**` and `**.md`, runs
      # nothing at all, and merges the one thing the test exists to reject.
      - '!doc/reference/**'
  workflow_dispatch:

# One matrix per PR head. A force-push supersedes the run it interrupted rather
# than racing it — the cheapest minutes are the ones spent on a stale commit.
concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

env:
  CARGO_TERM_COLOR: always
  # LESSON 2 of 3 — CI ONLY, never in `Cargo.toml`. CI's question is "does it
  # compile and do the tests pass", not "produce a shipping binary", so trade the
  # optimizer for headroom. `[profile.release]` uses thin LTO, and
  # link-time-optimizing several large test binaries at once is what exhausts a
  # hosted runner: the mirror's first published run died with SIGTERM 16 minutes
  # after its last line of output, mid-link, having never printed a single test
  # result. Set here rather than per-job so both legs inherit it.
  CARGO_PROFILE_RELEASE_LTO: "false"

jobs:
  # ── Leg 1: full Organon (the plugin, the standalone, the visual) ──────────────
  default-edition:
    name: build (default)
    runs-on: ubuntu-latest
    # A hang should fail loudly in an hour, not silently burn the 6-hour default.
    timeout-minutes: 90
    steps:
      - uses: actions/checkout@v4

      # LESSON 3a of 3 — the engine pulls in ALSA/JACK (nih-plug's standalone audio
      # backends) and X11/GL (baseview). See the contributor note in the header for
      # what skipping this looks like. The list is duplicated in CLAUDE.md's
      # toolchain section; keep the two in step.
      - name: Install system dev headers
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libasound2-dev libjack-jackd2-dev \
            libx11-dev libx11-xcb-dev libxcb1-dev libxcursor-dev libxrandr-dev \
            libxi-dev libxext-dev libgl1-mesa-dev libxkbcommon-dev libwayland-dev

      - uses: dtolnay/rust-toolchain@stable
      # LESSON 3b of 3 — `workspaces: native` because the crate is not at the repo
      # root. The two legs get DISTINCT cache keys: cargo fingerprints per feature
      # set, so a shared key would have each leg evicting artifacts the other just
      # wrote, and neither would ever hit.
      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: native
          key: default-edition

      # LESSON 1 of 3 — ONE profile and ONE feature set for both steps below, so
      # the dependency graph compiles once and is then reused. This matters more
      # than it looks: cargo fingerprints per feature set AND per profile, so
      # mixing them meant three full builds of a 125k-line crate in one job.
      #
      # ⚠️ Note `--release` on the test step. CLAUDE.md tells a human to run
      # `cargo build --release && cargo test`, which is a dev-profile test run and
      # therefore a second full compile of the whole dependency graph. That is
      # affordable on a warm laptop and is not affordable here. The test COUNT is
      # identical either way; only the compile is paid twice.
      - name: Build (default edition)
        working-directory: native
        run: cargo build --release

      # Includes `tests/wgsl.rs` — naga parse+validates every shader offline, the
      # closest thing to GPU coverage available without a GPU.

      # ⚠️ `--workspace` is LOAD-BEARING as of #626 Tier 3, not tidiness.
      # native/ is now a workspace whose root package is organic-math-native, and
      # a bare `cargo test` runs THE ROOT PACKAGE ONLY. When organon-core was
      # extracted, that silently stopped running its 44 tests — including
      # `editions_have_distinct_ipc_namespaces` in edition.rs, which is the one
      # guarding the invariant that lets an Organon session and a Console session
      # coexist in `$TMPDIR` without stomping each other's snapshot.
      # The suite went 1146 -> 1102 and stayed green, which is exactly how a
      # coverage loss hides. Every new workspace member is covered by this flag;
      # a per-crate `-p` list would have to be maintained and would rot.
      - name: Test (incl. offline WGSL validation)
        working-directory: native
        run: cargo test --release --workspace

      # 🚨 `--workspace` above does NOT reach `organon-module`'s `sim` feature, and
      # what hides behind it is not a corner: the simulator producer BINARY
      # (`required-features = ["sim"]`) and the whole cross-process staleness rig
      # (`tests/staleness.rs`, which is `#![cfg(feature = "sim")]` and therefore
      # compiles to an EMPTY FILE without it). Nothing in the workspace enables the
      # feature, so both would merge green and unbuilt.
      #
      # ⚠️ This is the seven-leg hole of #626 T3 in a second costume, and it was found
      # the same way — by asking which configurations nobody compiles rather than by
      # anything failing. `organonart/ascent` hit the identical shape on the other side
      # of this same boundary on the same day: its whole producer loop sat behind a
      # `hosted` feature and its licence job ran only `cargo tree`, which never invokes
      # rustc.
      #
      # 📌 `check --all-targets`, not `test`: the rig's two tests are `#[ignore]`d
      # because they launch a process and measure wall-clock, so running them here
      # would buy nothing. Compiling them is the whole point — a measurement rig that
      # stops building is a measurement nobody can repeat.
      - name: Check the module simulator and its staleness rig (feature `sim`)
        working-directory: native
        run: cargo check --release -p organon-module --all-features --all-targets

  # ── Leg 2: the Organon Console edition (Console #3 T1) ───────────────────────
  # `console-edition` is default-off, so leg 1 compiles none of
  # `src/console_main.rs` and none of the `Edition::Console` arms — the asymmetry
  # the header opens with. Note the two spellings: the bin lives in the ROOT crate
  # (the window will render `World`, which lives there until #618) and the
  # compositor lib in the `organon-console` package, so `--bin organon-console` and
  # `-p organon-console` name two different things that happen to share a spelling.
  # `-p organon-console` will not produce the binary.
  console-edition:
    name: build (console-edition)
    runs-on: ubuntu-latest
    timeout-minutes: 90
    steps:
      - uses: actions/checkout@v4

      - name: Install system dev headers
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libasound2-dev libjack-jackd2-dev \
            libx11-dev libx11-xcb-dev libxcb1-dev libxcursor-dev libxrandr-dev \
            libxi-dev libxext-dev libgl1-mesa-dev libxkbcommon-dev libwayland-dev

      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: native
          key: console-edition

      - name: Build Organon Console
        working-directory: native
        run: cargo build --release --features console-edition --bin organon-console

      - name: Test (console-edition, incl. offline WGSL validation)
        working-directory: native
        run: cargo test --release --workspace --features console-edition

  # ── Leg 3: the Windows cross-check — both editions, on a Linux runner ────────
  # organon#658 Tier 1. The cheap half of the platform axis: 1x billing, no apt
  # step, no Windows machine. See the header for why it needs no system packages
  # and, more importantly, for the list of things it cannot see.
  windows-crosscheck:
    name: check (windows cross)
    runs-on: ubuntu-latest
    # Far below the build legs' 90: this compiles metadata only, never links.
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v4

      # No `Install system dev headers` step, deliberately — see the header. The
      # Windows target reaches WASAPI/Win32 through pure-Rust binding crates, so
      # the ALSA/X11 packages the Linux legs need have nothing to install here.
      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: x86_64-pc-windows-msvc

      # Its own cache key: cargo fingerprints per TARGET as well as per feature
      # set and profile, so sharing a key with a Linux leg would have each
      # evicting artifacts the other just wrote — the same trap the two build
      # legs avoid between themselves.
      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: native
          key: windows-crosscheck

      # `--all-targets` so tests and examples are checked too, not just the lib
      # and bins. A `cfg(unix)` slip inside a test module is still a slip.
      #
      # ⚠️ Note the profile: `check` runs on the DEV profile here, unlike the
      # build legs' `--release`. That is on purpose — `check` never links, so
      # there is no optimizer to trade away, and the dev profile is what a
      # contributor's local inner loop uses. Matching it keeps CI and the
      # contributor note above reproducing each other exactly.
      - name: Check (default edition, Windows target)
        working-directory: native
        run: cargo check --target x86_64-pc-windows-msvc --workspace --all-targets

      # Console #3 T1 — THE reason this job covers both editions while
      # `build (windows)` covers one: this is the 1x runner, so the Console's
      # Windows compile coverage comes from here, not from a second 4x leg. See the
      # spend block in the header.
      - name: Check (console-edition, Windows target)
        working-directory: native
        run: cargo check --target x86_64-pc-windows-msvc --workspace --all-targets --features console-edition

  # ── Leg 4: Windows, for real — the only leg that links and runs tests there ───
  # organon#658 Tier 1. The expensive half (~4x a Linux leg), so it is used only
  # for what ONLY it can do: produce actual binaries and execute the suite on
  # Windows. Default edition only; the Console's Windows compile coverage is the
  # cross-check leg's.
  windows:
    name: build (windows)
    runs-on: windows-latest
    timeout-minutes: 90
    steps:
      - uses: actions/checkout@v4

      # ⚠️ The checkout arrives with CRLF line endings — the repo's root
      # `.gitattributes` sets `* text=auto`. That is fine for Rust and WGSL, and
      # it is NOT fine for the `*.sh` scripts, which is why organon#658 Tier 1
      # also pins those to LF in a separate change. Nothing in this job runs a
      # shell script, so the two are independent; if this leg ever goes red with
      # a `bad interpreter` or `\r` in an error message, that is the coupling
      # and the `.gitattributes` fix is what closes it.
      # organon#658 Tier 3 — the deploy scripts are PowerShell, and PowerShell is
      # only ever parsed on the machine that runs it. `bundle.ps1` / `deploy.ps1`
      # are therefore the one part of this repo whose FIRST syntax check would
      # otherwise be a person on a Windows box trying to deploy, which is the worst
      # possible moment to discover a stray brace. This step is seconds, needs no
      # toolchain, and runs before the 45-minute build so the feedback is immediate.
      #
      # It parses via the AST rather than executing: both scripts guard on Windows
      # and would happily start a real `cargo build` if simply run. Dot-sourcing
      # bundle.ps1 then asserting `Find-PluginBinaryDir` exists is a second, smaller
      # check — it proves the "run unless dot-sourced" gate at the bottom of that
      # file still works, which is what keeps its helpers testable without building.
      #
      # 🚨 THE ENCODING CHECK BELOW IS NOT DECORATION — the AST parse above cannot
      # replace it, and for four months it did not. This step runs under `pwsh`
      # (PowerShell 7), which reads a .ps1 as UTF-8 by default. **Windows PowerShell
      # 5.1 reads a BOM-less .ps1 as ANSI/CP1252**, and that is what ships in the box:
      # 5.1 is the only PowerShell present on a stock Windows 11, and `pwsh` is a
      # separate install. So a BOM-less file holding non-ASCII parses clean HERE and
      # is unparseable THERE — this gate was green while `deploy.ps1` could not run
      # at all on the machine it exists to serve, which is the precise failure the
      # comment above says this step prevents.
      #
      # The mechanism is worth naming, because "it is only a comment character" is
      # the reason it looks harmless: `→` (U+2192) is UTF-8 `E2 86 92`, and read as
      # CP1252 the final byte becomes **U+2019, a right single quote** — which
      # PowerShell honours as a string delimiter. Ten arrows in a comment block are
      # ten stray delimiters; the parser ran off the end of the file hunting a
      # terminator and reported a missing brace 200 lines from the real cause.
      #
      # The rule is therefore about BYTES, not syntax: a .ps1 must be pure ASCII, or
      # carry a UTF-8 BOM so 5.1 is told what it is holding. Either is fine and the
      # repo picks the BOM, which keeps the house typography (→, ⚠️, em dashes).
      - name: Validate the PowerShell deploy scripts
        shell: pwsh
        working-directory: native
        run: |
          $failed = $false
          foreach ($f in @('bundle.ps1', 'deploy.ps1')) {
            $bytes = [IO.File]::ReadAllBytes((Resolve-Path $f))
            $hasBom = $bytes.Length -ge 3 -and
                      $bytes[0] -eq 0xEF -and $bytes[1] -eq 0xBB -and $bytes[2] -eq 0xBF
            $nonAscii = @($bytes | Where-Object { $_ -gt 0x7F }).Count
            if ($nonAscii -gt 0 -and -not $hasBom) {
              $failed = $true
              Write-Host "::error file=native/$f::holds $nonAscii non-ASCII byte(s) and has no UTF-8 BOM — Windows PowerShell 5.1 will read it as CP1252 and fail to parse it. Add a BOM or make the file pure ASCII."
            } else {
              $how = if ($hasBom) { 'BOM' } else { 'pure ASCII' }
              Write-Host "OK  native/$f encoding ($how)"
            }
            $errors = $null
            [System.Management.Automation.Language.Parser]::ParseFile(
              (Resolve-Path $f), [ref]$null, [ref]$errors) | Out-Null
            if ($errors.Count -gt 0) {
              $failed = $true
              Write-Host "::error file=native/$f::$($errors.Count) parse error(s)"
              foreach ($e in $errors) {
                Write-Host "::error file=native/$f,line=$($e.Extent.StartLineNumber)::$($e.Message)"
              }
            } else {
              Write-Host "OK  native/$f"
            }
          }
          if ($failed) { exit 1 }
          . ./bundle.ps1
          if (-not (Get-Command Find-PluginBinaryDir -ErrorAction SilentlyContinue)) {
            Write-Host '::error::dot-sourcing bundle.ps1 did not define Find-PluginBinaryDir'
            exit 1
          }
          Write-Host 'OK  bundle.ps1 dot-source gate'

      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: native
          key: windows

      # Same lesson 1 as the Linux legs: ONE profile and ONE feature set across
      # build and test, so the dependency graph compiles once and is reused.
      - name: Build (default edition, Windows)
        working-directory: native
        run: cargo build --release

      # The payload this whole leg exists for. `--workspace` is as load-bearing
      # here as anywhere else — it is what runs `organon-core`'s IPC suite, and
      # the IPC layer is where the known Windows runtime risks live (resizing a
      # file another process has mapped fails on Windows with
      # ERROR_USER_MAPPED_FILE, where Unix does not care). If this step is going
      # to find something, that is where.
      #
      # ⚠️ `--no-fail-fast` IS DELIBERATE, AND IT IS NOT TIDINESS. It was added
      # after this leg's first-ever run, which failed exactly one test — the
      # ERROR_USER_MAPPED_FILE case above, in `organon-mind` — and in doing so
      # STOPPED THE RUN, leaving every later target in the workspace unrun and
      # unknown. Three properties compound to make that the wrong default here:
      #
      #   1. Windows failures arrive in CLASSES, not singly. A platform rule Unix
      #      does not have (mapped files, path separators, line endings) tends to
      #      be violated in several places at once, and fail-fast reveals them one
      #      per run.
      #   2. This is the SLOWEST leg (~4x a Linux one). Each masked failure costs
      #      a full round trip to learn, so N clustered failures cost N x 45min
      #      instead of one run and one fix.
      #   3. It is nearly free. The test step is ~3 minutes of a ~45-minute job,
      #      and running the remainder after a failure adds a rounding error.
      #
      # The Linux legs deliberately do NOT carry this: they are fast, they are
      # long-established, and a failure there is far more likely to be the one
      # thing the PR just broke than a platform-wide class.
      - name: Test (incl. offline WGSL validation, Windows)
        working-directory: native
        run: cargo test --release --workspace --no-fail-fast

  # ── Leg 5: macOS — the Console, the first macOS coverage this repo has ever had ─
  # See the macOS block in the header for why there is no cheap Apple
  # cross-check leg beside this one (`coreaudio-sys` runs bindgen against the macOS
  # SDK, so a Linux `cargo check` on the root crate never reaches our code), why it
  # is console-edition only, and what it does NOT prove about the default edition.
  macos:
    name: build (macos)
    runs-on: macos-latest
    timeout-minutes: 90
    steps:
      - uses: actions/checkout@v4

      # No `Install system dev headers` step, and unlike the Windows cross-check
      # this is not because nothing needs headers — `coreaudio-sys` and
      # `coremidi-sys` very much do. It is because a macOS runner ships with the
      # Xcode command-line tools and the SDK already, so `AudioUnit`, `CoreAudio`
      # and `CoreMIDI` resolve with nothing installed. The ALSA/JACK/X11 packages
      # the Linux legs install have no macOS counterpart to add: CoreAudio replaces
      # the first pair, and Cocoa/Metal the rest. `jack-sys` is still in the graph
      # here (it is a Unix dependency of nih-plug's standalone backends) and needs
      # nothing — it is `dlopen`-based, so it compiles with no JACK installed.
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: native
          key: macos

      # Same lesson 1 as every other leg: ONE profile and ONE feature set across
      # build and test, so the dependency graph compiles once and is reused.
      #
      # `--bin organon-console` is what asks for the Console front-of-house
      # specifically (the bin lives in the ROOT package and carries
      # `required-features`; `-p organon-console` names the compositor lib and will
      # not produce it — see leg 2b). It builds the root crate's lib too, which is
      # why this one command covers nih_plug, wgpu-on-Metal, winit and
      # `window_macos.rs` despite being scoped to the Console.
      - name: Build Organon Console (macOS)
        working-directory: native
        run: cargo build --release --features console-edition --bin organon-console

      # ⚠️ `--no-fail-fast` for the same reason `build (windows)` carries it, and
      # with more force: this is the FIRST macOS run this repository has ever had,
      # so if anything is wrong it is far more likely to be a platform-wide class
      # (a `cfg` arm, a path assumption, a PTY behaviour) than one isolated defect.
      # Learning those one per round trip is the failure mode the flag exists to
      # prevent. Drop it only once this leg has a history of being green.
      - name: Test (console-edition, incl. offline WGSL validation, macOS)
        working-directory: native
        run: cargo test --release --workspace --features console-edition --no-fail-fast
