### PBR text — a coordinator handoff, so the next session starts with the chair, not the history

`doc/pbr_text_handoff.md` is transient by design: the state of organon#217 as the first coordinator left it, written for a fresh session to take over with no other context. It carries James's standing rules for the work (subagents only — no chips; the demo says ORGANON; frames show a black room like the screensaver), the tier table with its PR numbers, what was in flight, what is open in order of value, the mechanics of the loop that cost time to learn (CI watching without `jq`, one review round, gating `main` after every merge, the §15-table conflicts), how to run the demo on the GPU, and the traps this machine and this harness have already charged for. §7 is the prompt to paste. Delete the file when the project ships; the durable state stays in `doc/pbr_text_engine.md` and the issue.
